# razorpayhack
An AI agent that browses a live storefront and completes real purchases on Razorpay, every transaction explainable, bounded by a hardcoded spending limit, and gated from executing anything it shouldn't.
